// ──────────────────────────────────────────────────────────────────────────
// 第 6 课用的「知识库」：一份【虚构】的公司内部文档。
//
// 为什么要虚构？—— 因为 RAG 的全部意义就是回答「模型预训练时没见过」的问题：
//   · 私有数据（公司内网、用户手册、最新政策）
//   · 实时数据（今天的库存、本月的价格）
//   · 长尾事实（模型记不住的细节数字）
// 这些事实里的数字/规则都是我编的，所以模型【一定不知道】，
// 只能靠「检索 → 喂给模型」才能答对。这样才能看清 RAG 到底做了什么。
// ──────────────────────────────────────────────────────────────────────────

export interface Doc {
  id: string;
  title: string;
  text: string;
}

// 虚构产品：「织云对象存储 ZeroStore」的内部知识库（FAQ + 政策）
export const DOCS: Doc[] = [
  {
    id: "pricing",
    title: "ZeroStore 计费规则",
    text: `ZeroStore 标准存储定价为每 GB 每月 0.018 元。低频访问存储为每 GB 每月 0.009 元，但取回时额外收取每 GB 0.03 元的取回费。
公网下行流量每 GB 0.5 元，同地域内网流量免费。
每个账号每月前 50 GB 存储与前 10 GB 公网下行流量免费。`,
  },
  {
    id: "limits",
    title: "ZeroStore 配额与限制",
    text: `单个 Bucket 默认最多存放 1 亿个对象，单个对象最大支持 48.8 TB（需开启分片上传，分片大小 5 MB 到 5 GB）。
单账号默认可创建 100 个 Bucket，可提工单上调至 1000 个。
默认 API 限流为每秒 2000 次请求（QPS），突发可达 5000 QPS，持续超限会返回 429。`,
  },
  {
    id: "sla",
    title: "ZeroStore 服务等级协议 SLA",
    text: `标准存储的设计持久性为 99.9999999999%（12 个 9），服务可用性承诺为 99.95%。
当月可用性低于 99.95% 但不低于 99.0% 时，赔偿当月账单的 10% 代金券；低于 99.0% 时赔偿 30%。
赔偿需在故障发生后 60 天内提交工单申请，逾期视为放弃。`,
  },
  {
    id: "security",
    title: "ZeroStore 安全与加密",
    text: `ZeroStore 默认对所有对象启用服务端 AES-256 加密，密钥由 KMS 托管，可选客户自管密钥（BYOK）。
Bucket 默认私有，支持 STS 临时凭证、Referer 防盗链与 Bucket Policy 精细授权。
开启版本控制后，删除对象只会生成删除标记，可在 7 天内通过控制台一键恢复（回收站默认保留期 7 天，最长可配 180 天）。`,
  },
  {
    id: "migration",
    title: "ZeroStore 数据迁移",
    text: `从第三方云迁移可使用「织云迁移助手」，支持断点续传与增量同步，单任务最高 200 个并发线程。
迁移期间源站到 ZeroStore 的入站流量完全免费，仅在迁移完成后按标准存储计费。
官方建议单任务数据量不超过 500 TB；超过时拆分为多个任务并行执行。`,
  },
];
